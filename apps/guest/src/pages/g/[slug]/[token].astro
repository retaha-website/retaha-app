---
// Lite-Plan URL: app.retaha.de/g/[hotel-slug]/[token]
// Leitet auf den Standard-Token-Pfad weiter, der die Session auflöst.
const { token } = Astro.params;
if (!token) return Astro.redirect('/', 302);
return Astro.redirect(`/g/${token}`, 302);
---
