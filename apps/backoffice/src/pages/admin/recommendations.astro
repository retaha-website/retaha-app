---
// Sprint E7 Phase 3 — /admin/recommendations ist umgezogen nach
// /admin/action-cards (Action-Card-Editor). 308 Permanent Redirect
// für Bookmark-Safety. Naming-Konflikt aus E2 ist damit gelöst:
// "Empfehlungen" hieß historisch falsch — es ging um die Hero-Cards.
//
// (Echte Empfehlungen = hotel_place_picks aus Sprint E2.)
const url = new URL(Astro.request.url);
const target = `/admin/action-cards${url.search}`;
return Astro.redirect(target, 308);
---
