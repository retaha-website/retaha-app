---
const authUrl = import.meta.env.AUTH_APP_URL ?? 'https://auth.retaha.de';
const returnTo = Astro.url.searchParams.get('return_to');
const target = returnTo
  ? `${authUrl}/login?return_to=${encodeURIComponent(returnTo)}`
  : `${authUrl}/login`;

return Astro.redirect(target, 302);
---
