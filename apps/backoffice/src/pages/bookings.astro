---
// Sprint E3 Phase 3 — /admin/bookings ist umgezogen nach /app/bookings
// (Operations-Dashboard). Forward query-params damit Filter erhalten bleiben.
const url = new URL(Astro.request.url);
const target = `/app/bookings${url.search}`;
return Astro.redirect(target, 308);
---
