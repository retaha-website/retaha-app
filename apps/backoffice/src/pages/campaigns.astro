---
import Layout from '../layouts/Layout.astro';
import ComingSoonModal from '@retaha/ui/components/admin/ComingSoonModal.astro';
import { getUser, getUserHotels } from '@retaha/auth';

const user = await getUser(Astro.cookies, Astro.request);
if (!user) return Astro.redirect('/admin/login');

const hotels = await getUserHotels(Astro.cookies, Astro.request);
const hotel = hotels?.[0]?.hotel;
if (!hotel) return Astro.redirect('/onboarding/locale');
---

<Layout title="Kampagnen" activeTab="backoffice">
  <ComingSoonModal
    feature_name="kampagnen"
    feature_eyebrow="marketing · kampagnen"
    feature_description="Einzel-Kampagnen an bestimmte Gäste-Segmente senden — saisonal, anlassbezogen oder spontan."
    planned_release="Q3 2026"
  />
</Layout>
